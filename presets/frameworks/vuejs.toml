gitignore = [
    "node_modules/",
    "dist/",
    ".env.local"
]

[metadata]
type = "vuejs"
role = "framework"
description = "Vue.js Framework"
required_tools = ["npm"]

[scripts]
run = "npm run dev"
build = "npm run build"
serve = "npm run serve"
lint = "npm run lint"
install = "npm install"

[env]