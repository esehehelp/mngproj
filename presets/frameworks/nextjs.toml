gitignore = [
    ".next/",
    "out/",
    "build/",
    "dist/"
]

[metadata]
type = "nextjs"
role = "framework"
description = "Next.js React Framework"
required_tools = ["npm"]

[scripts]
dev = "next dev"
build = "next build"
start = "next start"
lint = "next lint"
run = "npm run dev"