[metadata]
type = "rust"
role = "language"
description = "Rust Programming Language (Cargo)"

[scripts]
run = "cargo run"
build = "cargo build --release"
test = "cargo test"
lint = "cargo clippy -- -D warnings"
clean = "cargo clean"
format = "cargo fmt"

[env]
RUST_BACKTRACE = "1"
